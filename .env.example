# Copy file này thành .env.local rồi điền giá trị thật.
# File .example ĐƯỢC commit — nó là tài liệu cho biết project cần biến gì.
# File .env.local KHÔNG commit.
#
# Local:      http://localhost:3003
# Production: https://ewallet-api-xxxx.onrender.com  (điền sau khi deploy Render)
VITE_API_URL=http://localhost:3003
